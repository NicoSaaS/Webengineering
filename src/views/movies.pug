extends layout.pug

block content
  div.body_container
    each group in groupedMovies
        h4.whiteFontColor= group.letter
        ul.media_list
          each movie in group.movies
            li
              img.media_cover(src=movie.image, alt=movie.title)
