extends layout.pug

block content
  div.body_container.marginLeft
    each group in groupedSerie 
        h4.whiteFontColor.marginTop= group.letter
        ul.media_list
          each serie in group.series 
            li(data-serie=JSON.stringify(serie))
              img.media_cover(src=serie.image, alt=serie.title, onclick="showSerieDetails(event)")

  div.popup(id="mediaPopup")
        div.popup-body
            div(id="movieModal", style="display: none;")
            div.modal-content
                span.close-btn &times;
                .popup-content
                    img.media_image(id="media-image", src="")
                    .media-information
                        .media-general
                            .media-header
                                h2(id="media-title")
                                button.bookmarkButton(data-movie=JSON.stringify(movie))
                                    img(src="/img/bookmark.png", alt="Bookmark")
                            p(id="media-description")
                            .media-specific-info
                                p(id="media-genre")
                                p(id="media-ranking")
                                p(id="media-director")
                                p(id="media-released")

  script(src="/js/serie.js")